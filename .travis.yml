sudo: required
language: java
jdk:
- oraclejdk8
services:
- docker

env:
  global:
  - SHORT_COMMIT=${TRAVIS_COMMIT::8}

script:
 - mvn --version
 - mvn verify || travis_terminate 1
 - ./travis-build.sh || travis_terminate 1